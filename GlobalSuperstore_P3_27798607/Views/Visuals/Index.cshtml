@model GlobalSuperstore_P3_27798607.Models.Visuals

@{
    ViewBag.Title = "Visuals";
}
@{int chartnum = 0;}


@*
    @Html.DropDownList("chart", new SelectListItem[]
        {
        new SelectListItem() { Text = "Location insights on Order Sales based on Country", Value = "1"},
        new SelectListItem() { Text = "insights on Order Sales based on Country", Value = "2"}
        },
        new {  @class = "form-control", @change= "dropChange(this.value)" })
*@

<div class="jumbotron">
    <h1 style="color:#00bc8c !important ">Data Visuals</h1>
    <p class="lead">
        Instructions on how to do stuff
</div>
<script>
    function dropChange(val) {
        if (val == "1") {
            alert('Hello, world! 1');
            chartnum = "1";
            reload();

        } else if (val == "2") {
            alert('Hello, world! 2');
            chartnum = "2";
        }
    }
</script>
@if (chartnum == 1)
{
    <div class="embed-responsive embed-responsive-16by9">
        <iframe class="embed-responsive-item" style="border: none;border-radius: 2px;box-shadow: 0 2px 10px 0 rgba(70, 76, 79, .2);" width="100%" height="60%" src="https://charts.mongodb.com/charts-itrw324-mvc-app-epfbp/embed/charts?id=8461a351-574f-4015-bf7a-0af5d778ccb2&tenant=26d149e0-7d17-4607-97c6-c6108148351b"></iframe>
    </div>
}
else if (chartnum == 2)
{ <h1 style="color:#00bc8c !important ">Data Visuals</h1>}
<select class="form-control" id="ddl" name="ddl" onmousedown="this.value='';" onchange="if(this.options.value='1'); {chartnum=1} else{chartnum=2}">
    <option value='1'>One</option>
    <option value='2'>Two</option>
</select>





@if (User.Identity.IsAuthenticated)
{
    <h2 style="color:#3498db">Current tools available:</h2>
    <div class="row">

        <div class="col-md-3">
            <h3>Manage Orders</h3>
            <p>BLAH BLAH BLAH</p>
            <p><a class="btn btn-info btn-sm" href="@Url.Action("Register", null, new { area = string.Empty, controller = "Account" }, Request.Url.Scheme)">Go there &raquo;</a></p>
        </div>
        <div class="col-md-3">
            <h3>Manage Returns</h3>
            <p>BLAH BLAH BLAH</p>
            @if (User.Identity.IsAuthenticated)
            {
                <p><a class="btn btn-info btn-sm" href="@Url.Action("Register", null, new { area = string.Empty, controller = "Account" }, Request.Url.Scheme)">Go there &raquo;</a></p>
            }
        </div>
        <div class="col-md-3">
            <h3>Manage People</h3>
            <p>BLAH BLAH BLAH</p>
            @if (User.Identity.IsAuthenticated)
            {
                <p><a class="btn btn-info btn-sm" href="@Url.Action("Register", null, new { area = string.Empty, controller = "Account" }, Request.Url.Scheme)">Go there &raquo;</a></p>
            }
        </div>
        <div class="col-md-3">
            <h3>Visualizations</h3>
            <p>This show cases the data BLAH BLAH BLAH</p>
            @if (User.Identity.IsAuthenticated)
            {
                <p><a class="btn btn-info btn-sm" href="@Url.Action("Register", null, new { area = string.Empty, controller = "Account" }, Request.Url.Scheme)">Go there  &raquo;</a></p>
            }
        </div>
    </div>
}
else
{ }
